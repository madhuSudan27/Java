package Enum;

public enum EnumSample {

    /*

    Normal enum class implementation

     */

    // if we did not define any value then internally a unique is assigned starting from 0
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY;
}
